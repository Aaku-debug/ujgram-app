/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[
            {hostname:"beaming-mouse-68.convex.cloud"},
        ]
    }
};

export default nextConfig;
